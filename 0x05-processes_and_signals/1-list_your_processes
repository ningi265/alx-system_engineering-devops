#!/usr/bin/env bash
# Display a list of currently running processes

# Use the ps command to list all processes in a user-oriented format
# The -e option selects all processes, and -o specifies the output format
# The f option shows the process hierarchy (process tree)
ps -e -o pid,ppid,user,%cpu,%mem,cmd --forest

